/*
 * 如果派生类的方法Foo实际上没有override任何基类的虚方法，编译器就会产生一个错误。但如果你不添加override修饰符，编译器则不进行任何检查.
 * enum class Color可以将枚举值至于枚举类型的scope之下，即必须使用Color::red这个格式。
 * 其中unordered_map和unordered_set都是C++11新增的标准容器。
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
*/
